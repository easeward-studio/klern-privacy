# Savense Privacy

Public Privacy Policy for **Savense**, published by **Easeward Studio**.

**Tagline:** Where saving makes sense.

The site is deployed with GitHub Pages from the `main` branch. The repository was originally created under the Klern working name; rename the repository to `savense-privacy` in GitHub Settings before the final store submission so the public Pages URL also uses the Savense name.
